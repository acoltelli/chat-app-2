# chat-app
Chat app that allows user to pick a username and join an already existing chatroom or create their own.

## Stack
This project is built using Flask and [Socket.IO](https://socket.io/), a javascript library which enables realtime communication between web clients and servers using the [WebSocket](https://en.wikipedia.org/wiki/WebSocket) protocol.
